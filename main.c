#include <stdio.h>

int main() {
    int a ;
    int b ;
    printf("\n moi nhap so a: ");
    scanf( "%d", &a);
    printf("\n moi nhap so b: ");
    scanf( "%d", &b);
    printf( "\n %d + %d =%d ",a,b,a+b );
    // kết thúc chương trình
    return 0;
}