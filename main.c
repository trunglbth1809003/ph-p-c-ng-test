#include <stdio.h>

int main() {
    int a;
    int b;
    printf(" moi nhap so a: ");
    scanf("%d", &a);
    printf("moi nhap so b: ");
    scanf("%d", &b);
    printf(" %d + %d =%d ", a, b, a + b);
    // kết thúc chương trình
    return 0;
}